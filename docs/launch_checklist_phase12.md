# Launch Checklist (Phase 12)

- [ ] Backend CI passing with all gates (`backend.yml`).
- [ ] Web CI passing with API generation + critical Playwright checks (`web-ci.yml`).
- [ ] Migration up/down validation passed in CI.
- [ ] Coverage artifact generated and reviewed.
- [ ] OpenAPI contract checks passing.
- [ ] Docker image build passing.
- [ ] Vulnerability scans reviewed (govulncheck/gosec/trivy).
- [ ] Staging deploy + smoke passed.
- [ ] Production deploy runbook reviewed and rollback tested.
- [ ] Payment approval runbook verified with operations team.
- [ ] Incident response runbook reviewed on-call.
