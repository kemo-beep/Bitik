import { HomeClient } from "@/app/(storefront)/home-client"

export const metadata = {
  title: "Bitik",
  description: "Discover products, categories, and brands on Bitik.",
}

export default function Page() {
  return <HomeClient />
}
